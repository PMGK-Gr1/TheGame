using UnityEngine;
using System.Collections;

public class upgradecountingame : MonoBehaviour {

	// Use this for initialization
	void Start () {
        this.guiTexture.pixelInset = new Rect(Screen.width * 0.145f, Screen.height * 0.05f, Screen.height * 0.15f, Screen.height * 0.1f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
